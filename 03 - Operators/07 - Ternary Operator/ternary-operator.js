// Declaring two numbers
let a = 10;
let b = 5;

// Using the ternary operator (condition ? expression1 : expression2)
let result = (a > b) ? "a is greater" : "b is greater";
console.log("Ternary Operator Result:", result); // Output: a is greater

// Checking if a number is even or odd
let num = 7;
let isEven = (num % 2 === 0) ? "Even" : "Odd";
console.log(num, "is", isEven); // Output: 7 is Odd