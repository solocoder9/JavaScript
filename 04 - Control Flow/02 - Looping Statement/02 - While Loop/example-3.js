// Example 3: Looping through an array using while loop
let fruits = ["Apple", "Banana", "Cherry"];
let index = 0;

while (index < fruits.length) {
    console.log(fruits[index]); 
    // Output:
    // Apple
    // Banana
    // Cherry
    index++;
}