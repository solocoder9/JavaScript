// Example 3: Looping through an array using do-while loop
let fruits = ["Apple", "Banana", "Cherry"];
let index = 0;

do {
    console.log(fruits[index]); 
    // Output:
    // Apple
    // Banana
    // Cherry
    index++;
} while (index < fruits.length);